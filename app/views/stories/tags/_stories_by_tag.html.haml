.stories-index__content.col-12
  .stories-selection.card-columns
    - @stories_by_tag.each do |story|
      .card
        = link_to(image_tag(story.image, width: '100%', alt: "##{story.name} #DistritoRugby", class: 'img-hov' ), "/stories/#{story.id}/tags/#{label}")
        .card-body
          %h5.card-title= link_to story.name, "/stories/#{story.id}/tags/#{label}", class: 'link'
          %p.card-text= story.summary

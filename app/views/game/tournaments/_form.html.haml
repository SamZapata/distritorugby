-# frozen_string_literal: true
= simple_form_for(@game_tournament) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.input :name
    = f.input :start_date
    = f.input :end_date
    = f.input :about
    = f.input :country
    = f.input :region
    = f.input :city
    = f.input :organizer

  .form-actions
    = f.button :submit
